import ListMarketDataProviders from './ListMarketDataProviders'
import CreateMarketDataProvider from './CreateMarketDataProvider'
import EditMarketDataProvider from './EditMarketDataProvider'
const Pages = {
    ListMarketDataProviders: Object.assign(ListMarketDataProviders, ListMarketDataProviders),
CreateMarketDataProvider: Object.assign(CreateMarketDataProvider, CreateMarketDataProvider),
EditMarketDataProvider: Object.assign(EditMarketDataProvider, EditMarketDataProvider),
}

export default Pages