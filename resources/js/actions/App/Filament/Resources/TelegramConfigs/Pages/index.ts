import ListTelegramConfigs from './ListTelegramConfigs'
import CreateTelegramConfig from './CreateTelegramConfig'
import EditTelegramConfig from './EditTelegramConfig'
const Pages = {
    ListTelegramConfigs: Object.assign(ListTelegramConfigs, ListTelegramConfigs),
CreateTelegramConfig: Object.assign(CreateTelegramConfig, CreateTelegramConfig),
EditTelegramConfig: Object.assign(EditTelegramConfig, EditTelegramConfig),
}

export default Pages