import Achievements from './Achievements'
import AffiliateCodes from './AffiliateCodes'
import AffiliateRewards from './AffiliateRewards'
import AiProviderModels from './AiProviderModels'
import AiProviders from './AiProviders'
import Features from './Features'
import Feedback from './Feedback'
import Invitations from './Invitations'
import JournalEntries from './JournalEntries'
import Languages from './Languages'
import MarketDataProviders from './MarketDataProviders'
import NewsProviders from './NewsProviders'
import PlanChanges from './PlanChanges'
import PricingPlans from './PricingPlans'
import Referrals from './Referrals'
import Roles from './Roles'
import TelegramConfigs from './TelegramConfigs'
import TradingProviders from './TradingProviders'
import UserTradingStats from './UserTradingStats'
import Users from './Users'
import WaitlistEntries from './WaitlistEntries'
const Resources = {
    Achievements: Object.assign(Achievements, Achievements),
AffiliateCodes: Object.assign(AffiliateCodes, AffiliateCodes),
AffiliateRewards: Object.assign(AffiliateRewards, AffiliateRewards),
AiProviderModels: Object.assign(AiProviderModels, AiProviderModels),
AiProviders: Object.assign(AiProviders, AiProviders),
Features: Object.assign(Features, Features),
Feedback: Object.assign(Feedback, Feedback),
Invitations: Object.assign(Invitations, Invitations),
JournalEntries: Object.assign(JournalEntries, JournalEntries),
Languages: Object.assign(Languages, Languages),
MarketDataProviders: Object.assign(MarketDataProviders, MarketDataProviders),
NewsProviders: Object.assign(NewsProviders, NewsProviders),
PlanChanges: Object.assign(PlanChanges, PlanChanges),
PricingPlans: Object.assign(PricingPlans, PricingPlans),
Referrals: Object.assign(Referrals, Referrals),
Roles: Object.assign(Roles, Roles),
TelegramConfigs: Object.assign(TelegramConfigs, TelegramConfigs),
TradingProviders: Object.assign(TradingProviders, TradingProviders),
UserTradingStats: Object.assign(UserTradingStats, UserTradingStats),
Users: Object.assign(Users, Users),
WaitlistEntries: Object.assign(WaitlistEntries, WaitlistEntries),
}

export default Resources